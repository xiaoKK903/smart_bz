# 电商售后插件设计

> 通用智能客服平台的第二个领域实例。  
> 与八字插件形成对照，证明平台插件架构的通用性。

---

## 一、场景分析

### 1.1 电商售后的痛点

| 痛点 | 现状 | AI 解决方案 |
|------|------|------------|
| 重复咨询占比高 | 70-80% 的问题是"物流到哪了""怎么退款" | FAQ + RAG 自动回答 |
| 人力成本高 | 一个客服坐席月薪 4000-6000 | AI 替代 80% 简单咨询 |
| 响应慢 | 排队等待 5-30 分钟 | AI 秒回，7×24 |
| 夜间无人值守 | 晚 10 点到早 8 点无客服 | AI 全时段覆盖 |
| 服务质量不稳定 | 取决于客服个人水平和情绪 | AI 标准化输出 |

### 1.2 核心业务指标

| 指标 | 说明 | AI 目标 |
|------|------|---------|
| **首次解决率 (FCR)** | 第一次对话就解决问题 | >75% |
| **平均处理时长 (AHT)** | 从发起到解决的时间 | <3 分钟 |
| **转人工率** | AI 无法处理转人工的比例 | <20% |
| **客户满意度 (CSAT)** | 对话结束评分 | >4.2/5 |
| **成本节省率** | 对比纯人工的成本降幅 | >60% |

---

## 二、意图体系

### 2.1 意图树（三级）

```
电商售后
├── 订单类
│   ├── order_status        # 查询订单状态
│   ├── order_modify        # 修改订单（地址、数量）
│   └── order_cancel        # 取消订单
│
├── 物流类
│   ├── logistics_query     # 查物流进度
│   ├── logistics_delay     # 物流延迟投诉
│   └── logistics_lost      # 包裹丢失
│
├── 退换货类
│   ├── refund_request      # 申请退款
│   ├── return_request      # 申请退货
│   ├── exchange_request    # 申请换货
│   └── refund_status       # 查退款进度
│
├── 商品类
│   ├── product_consult     # 商品咨询（规格、用法）
│   ├── product_complaint   # 商品质量投诉
│   └── product_recommend   # 商品推荐
│
├── 优惠/支付类
│   ├── coupon_query        # 优惠券查询/使用
│   ├── payment_issue       # 支付问题
│   └── invoice_request     # 开发票
│
└── 其他
    ├── complaint           # 一般投诉
    ├── human_agent         # 转人工
    └── chitchat            # 闲聊
```

### 2.2 意图识别策略

```python
# 意图识别采用"规则 + LLM"混合模式
# 高频简单意图用规则（快+省Token），复杂/模糊意图用LLM

RULE_PATTERNS = {
    "logistics_query": [
        r"物流|快递|到哪了|发货了吗|运单|tracking",
        r"什么时候到|几天到|到了吗"
    ],
    "refund_request": [
        r"退款|退钱|不想要了|申请退",
    ],
    "refund_status": [
        r"退款.*进度|退款.*到账|退了吗|钱.*退.*了吗"
    ],
    "order_status": [
        r"订单.*状态|订单.*到哪|我的订单"
    ],
    "human_agent": [
        r"转人工|人工客服|真人|找个人"
    ]
}

# 规则未命中 → 走 LLM 意图分类（带 few-shot 示例）
```

---

## 三、插件配置

### 3.1 config.yaml

```yaml
# domains/ecommerce/config.yaml
name: "电商售后客服"
version: "1.0.0"
description: "电商售后场景智能客服，覆盖订单、物流、退换货、商品咨询"

# ─────────────── 意图与槽位 ───────────────

intents:
  # ── 订单查询 ──
  - id: "order_status"
    name: "订单状态查询"
    description: "查询订单当前状态"
    required_slots:
      - id: "order_id"
        type: "string"
        pattern: "^[A-Z0-9]{10,20}$"
        prompt: "请提供您的订单号，我帮您查询。"
        extract_hint: "订单号通常是10-20位的数字字母组合"
    fallback_slots:
      - id: "phone"
        type: "phone"
        prompt: "也可以提供下单时的手机号，我帮您查找。"

  # ── 物流查询 ──
  - id: "logistics_query"
    name: "物流进度查询"
    description: "查询包裹物流信息"
    required_slots:
      - id: "order_id"
        type: "string"
        prompt: "请提供订单号，我帮您查看物流进度。"
    optional_slots:
      - id: "logistics_company"
        type: "enum"
        values: ["顺丰", "中通", "圆通", "韵达", "申通", "京东", "邮政"]
        prompt: "方便告知是哪家快递吗？"

  # ── 退款申请 ──
  - id: "refund_request"
    name: "申请退款"
    description: "用户申请退款"
    required_slots:
      - id: "order_id"
        type: "string"
        prompt: "请提供需要退款的订单号。"
      - id: "refund_reason"
        type: "enum"
        values:
          - "不想要了/多拍了"
          - "商品质量问题"
          - "与描述不符"
          - "未收到货"
          - "发错货"
          - "其他"
        prompt: "请选择退款原因：\n1. 不想要了/多拍了\n2. 商品质量问题\n3. 与描述不符\n4. 未收到货\n5. 发错货\n6. 其他"
    optional_slots:
      - id: "refund_amount"
        type: "money"
        prompt: "需要退全款还是部分退款？"
      - id: "refund_evidence"
        type: "image"
        prompt: "如果是质量问题，请上传商品照片作为凭证。"
    confirmation_required: true
    confirm_prompt: "确认为订单 {order_id} 申请退款，原因：{refund_reason}。是否提交？"

  # ── 退货申请 ──
  - id: "return_request"
    name: "申请退货"
    description: "用户申请退货退款"
    required_slots:
      - id: "order_id"
        type: "string"
        prompt: "请提供需要退货的订单号。"
      - id: "return_reason"
        type: "enum"
        values: ["质量问题", "尺码不合适", "颜色/款式不满意", "与描述不符", "其他"]
        prompt: "请问退货原因是？"
    optional_slots:
      - id: "return_evidence"
        type: "image"
        prompt: "方便拍照提供商品图片吗？"
    confirmation_required: true

  # ── 商品咨询 ──
  - id: "product_consult"
    name: "商品咨询"
    description: "商品规格、使用方法等咨询"
    required_slots:
      - id: "product_name"
        type: "string"
        prompt: "您想咨询哪款商品？"
    optional_slots:
      - id: "question_type"
        type: "enum"
        values: ["规格参数", "使用方法", "适用范围", "保修政策", "其他"]

  # ── 投诉 ──
  - id: "complaint"
    name: "投诉"
    description: "用户投诉"
    required_slots:
      - id: "complaint_content"
        type: "text"
        prompt: "请描述您遇到的问题，我们会尽快为您处理。"
    auto_escalate: true
    escalate_after: 2  # 投诉类2轮后自动转人工

# ─────────────── 模型配置 ───────────────

model:
  preferred: "deepseek-chat"
  temperature: 0.3          # 客服场景要稳定，低温度
  max_tokens: 800           # 客服回复不宜太长
  
  # 意图级模型覆盖
  overrides:
    product_consult:
      temperature: 0.5      # 商品咨询可以稍微发散
      max_tokens: 1200
    complaint:
      preferred: "deepseek-v3"  # 投诉用更强的模型
      temperature: 0.2

# ─────────────── 外部集成 ───────────────

integrations:
  order_api:
    base_url: "${ORDER_API_BASE}"
    auth: "bearer ${ORDER_API_TOKEN}"
    endpoints:
      get_order: "GET /api/orders/{order_id}"
      cancel_order: "POST /api/orders/{order_id}/cancel"
      get_refund_status: "GET /api/refunds/{order_id}"
  
  logistics_api:
    base_url: "${LOGISTICS_API_BASE}"
    endpoints:
      query: "GET /api/track/{tracking_no}"
  
  product_api:
    base_url: "${PRODUCT_API_BASE}"
    endpoints:
      search: "GET /api/products/search?q={query}"
      detail: "GET /api/products/{product_id}"

# ─────────────── 知识库 ───────────────

knowledge:
  sources:
    - type: "file"
      path: "knowledge/退换货政策.md"
      priority: high
    - type: "file"
      path: "knowledge/常见问题FAQ.md"
      priority: high
    - type: "file"
      path: "knowledge/商品目录.md"
      priority: medium
    - type: "file"
      path: "knowledge/物流说明.md"
      priority: medium
    - type: "qa_pairs"
      path: "knowledge/qa_pairs.json"
      priority: highest    # 精确QA对最高优先级

# ─────────────── 人机转接规则 ───────────────

handoff:
  triggers:
    - type: "intent"
      value: "complaint"
      after_rounds: 2           # 投诉2轮后转
    - type: "keyword"
      values: ["投诉", "315", "消协", "工商", "曝光", "起诉"]
      immediate: true           # 关键词立即转
    - type: "sentiment"
      threshold: -0.7           # 情绪极度负面时转
    - type: "confidence"
      threshold: 0.4
      consecutive: 3            # 连续3轮低置信度转
    - type: "loop"
      max_rounds: 8             # 超过8轮未解决转
  
  queue_config:
    max_wait: 300               # 最长排队5分钟
    overflow_message: "当前客服繁忙，已记录您的问题，将在30分钟内回复您。"

# ─────────────── UI 配置 ───────────────

ui:
  theme_color: "#FF6B35"
  welcome_message: "您好！我是智能客服小助手，可以帮您查订单、查物流、处理退换货。请问有什么可以帮您？"
  avatar: "assets/ecommerce_avatar.png"
  quick_replies:
    - "查询订单"
    - "查物流"
    - "申请退款"
    - "商品咨询"
    - "转人工"
```

### 3.2 插件核心代码

```python
# domains/ecommerce/plugin.py

from domains.base import BaseDomainPlugin, Intent, Slot, Session, Tool
from typing import Optional
import httpx


class EcommercePlugin(BaseDomainPlugin):
    """电商售后领域插件"""

    def __init__(self, config: dict):
        super().__init__(config)
        self.order_client = httpx.AsyncClient(
            base_url=config["integrations"]["order_api"]["base_url"],
            headers={"Authorization": config["integrations"]["order_api"]["auth"]}
        )

    # ─────────── 接口实现 ───────────

    def get_intents(self) -> list[Intent]:
        """从 config.yaml 加载意图定义"""
        return [Intent.from_yaml(i) for i in self.config["intents"]]

    def get_slots(self, intent: str) -> list[Slot]:
        intent_config = self._find_intent(intent)
        slots = [Slot.from_yaml(s) for s in intent_config.get("required_slots", [])]
        slots += [Slot.from_yaml(s, required=False) for s in intent_config.get("optional_slots", [])]
        return slots

    def build_context(self, session: Session) -> str:
        """构建电商特定上下文"""
        context_parts = []

        # 1. 如果有订单号，拉取订单详情
        order_id = session.get_slot("order_id")
        if order_id:
            order_info = self._format_order_info(
                session.cache.get("order_data")
            )
            if order_info:
                context_parts.append(f"【订单信息】\n{order_info}")

        # 2. 如果有物流信息
        logistics = session.cache.get("logistics_data")
        if logistics:
            context_parts.append(f"【物流信息】\n{self._format_logistics(logistics)}")

        # 3. 用户历史标签
        user_tags = session.user_profile.get("tags", [])
        if user_tags:
            context_parts.append(f"【用户标签】{', '.join(user_tags)}")

        # 4. 是否VIP
        if session.user_profile.get("vip_level"):
            context_parts.append(f"【VIP等级】{session.user_profile['vip_level']}（请优先处理）")

        return "\n\n".join(context_parts)

    def get_system_prompt(self, intent: str) -> str:
        """返回电商客服 System Prompt"""
        base_prompt = """你是一位专业的电商售后客服。请遵循以下原则：

## 角色
- 友好、耐心、专业
- 始终站在客户角度思考
- 快速定位问题并给出解决方案

## 规则
1. 先确认用户问题，再给方案
2. 涉及退款/退货，必须先查订单状态再处理
3. 金额、时效等关键信息必须准确，不确定时说明
4. 不要承诺超出权限的事情（如"立即退款"）
5. 情绪激动的用户，先安抚再解决
6. 复杂问题超出能力范围，引导转人工

## 话术规范
- 禁止说"这不是我们的问题"
- 禁止说"你自己去看"
- 使用"我帮您查看""为您处理""非常抱歉给您带来不便"
- 回复简洁，避免大段文字，分步骤说明"""

        # 按意图追加特定指令
        intent_prompts = {
            "refund_request": """
## 退款处理流程
1. 确认订单号和退款原因
2. 查询订单状态（已发货/未发货/已签收）
3. 根据退换货政策判断是否符合条件
4. 符合 → 引导提交退款申请
5. 不符合 → 解释原因，提供替代方案""",

            "complaint": """
## 投诉处理流程
1. 认真倾听，表达歉意
2. 确认问题细节
3. 给出解决方案或补偿建议
4. 如用户不满意，告知将升级处理
5. 记录投诉内容""",

            "product_consult": """
## 商品咨询
1. 基于知识库中的商品信息回答
2. 不确定的参数不要瞎编
3. 可以适当推荐相关商品
4. 引导用户下单（但不要过度推销）"""
        }

        extra = intent_prompts.get(intent, "")
        return base_prompt + extra

    def post_process(self, response: str, session: Session) -> str:
        """后处理：添加快捷操作按钮"""
        intent = session.current_intent

        # 退款/退货场景，追加操作链接
        if intent in ("refund_request", "return_request"):
            order_id = session.get_slot("order_id")
            if order_id and "确认" not in response:
                response += f"\n\n📋 [点击提交退款申请]({self.config['ui']['refund_form_url']}?order={order_id})"

        # 物流查询，追加物流轨迹链接
        if intent == "logistics_query":
            tracking_no = session.cache.get("tracking_no")
            if tracking_no:
                response += f"\n\n📦 [查看完整物流轨迹](https://www.kuaidi100.com/chaxun?nu={tracking_no})"

        return response

    def get_tools(self) -> list[Tool]:
        """声明领域工具（Function Calling）"""
        return [
            Tool(
                name="query_order",
                description="根据订单号查询订单详情（状态、金额、商品、收货地址）",
                parameters={
                    "order_id": {"type": "string", "description": "订单号"}
                },
                handler=self._tool_query_order
            ),
            Tool(
                name="query_logistics",
                description="根据订单号查询物流轨迹",
                parameters={
                    "order_id": {"type": "string", "description": "订单号"}
                },
                handler=self._tool_query_logistics
            ),
            Tool(
                name="submit_refund",
                description="提交退款申请",
                parameters={
                    "order_id": {"type": "string", "description": "订单号"},
                    "reason": {"type": "string", "description": "退款原因"},
                    "amount": {"type": "number", "description": "退款金额（可选，默认全额）"}
                },
                handler=self._tool_submit_refund,
                requires_confirmation=True  # 需要用户确认
            ),
            Tool(
                name="search_product",
                description="搜索商品信息",
                parameters={
                    "query": {"type": "string", "description": "搜索关键词"}
                },
                handler=self._tool_search_product
            ),
        ]

    # ─────────── 工具实现 ───────────

    async def _tool_query_order(self, order_id: str) -> dict:
        """查询订单"""
        resp = await self.order_client.get(f"/api/orders/{order_id}")
        if resp.status_code == 404:
            return {"error": "未找到该订单，请确认订单号是否正确"}
        data = resp.json()
        return {
            "order_id": data["id"],
            "status": data["status"],           # pending/paid/shipped/delivered/completed/cancelled
            "status_text": self._status_text(data["status"]),
            "total_amount": data["total_amount"],
            "items": [{"name": i["name"], "qty": i["quantity"], "price": i["price"]} for i in data["items"]],
            "created_at": data["created_at"],
            "shipping_address": data.get("shipping_address", ""),
        }

    async def _tool_query_logistics(self, order_id: str) -> dict:
        """查询物流"""
        resp = await self.order_client.get(f"/api/orders/{order_id}/logistics")
        if resp.status_code != 200:
            return {"error": "暂无物流信息"}
        data = resp.json()
        return {
            "company": data["company"],
            "tracking_no": data["tracking_no"],
            "status": data["status"],
            "latest": data["tracks"][0] if data["tracks"] else None,
            "estimated_delivery": data.get("estimated_delivery"),
            "tracks": data["tracks"][:5],  # 最近5条轨迹
        }

    async def _tool_submit_refund(self, order_id: str, reason: str, amount: float = None) -> dict:
        """提交退款"""
        payload = {"order_id": order_id, "reason": reason}
        if amount:
            payload["amount"] = amount
        resp = await self.order_client.post("/api/refunds", json=payload)
        if resp.status_code == 200:
            data = resp.json()
            return {
                "success": True,
                "refund_id": data["refund_id"],
                "estimated_time": "1-3个工作日",
                "message": f"退款申请已提交（单号：{data['refund_id']}），预计1-3个工作日到账。"
            }
        else:
            return {"success": False, "error": resp.json().get("message", "提交失败，请稍后重试")}

    async def _tool_search_product(self, query: str) -> dict:
        """搜索商品"""
        # 先查知识库，再查商品API
        rag_results = await self.rag.search(query, top_k=3)
        return {"products": rag_results}

    # ─────────── 生命周期 ───────────

    def on_session_start(self, session: Session):
        """会话开始：设置快捷回复"""
        session.set_quick_replies([
            "查询订单", "查物流", "申请退款", "商品咨询", "转人工"
        ])

    def on_session_end(self, session: Session):
        """会话结束：生成会话摘要、更新用户标签"""
        # 自动摘要
        summary = self._generate_summary(session)
        session.save_summary(summary)

        # 更新用户标签
        if session.current_intent == "complaint":
            session.user_profile.add_tag("有投诉记录")
        if session.current_intent == "refund_request":
            session.user_profile.increment("refund_count")

    # ─────────── 辅助方法 ───────────

    @staticmethod
    def _status_text(status: str) -> str:
        return {
            "pending": "待付款",
            "paid": "已付款，待发货",
            "shipped": "已发货，运输中",
            "delivered": "已签收",
            "completed": "已完成",
            "cancelled": "已取消",
            "refunding": "退款中",
            "refunded": "已退款",
        }.get(status, status)

    @staticmethod
    def _format_order_info(order: dict) -> str:
        if not order:
            return ""
        items_text = "\n".join(
            f"  - {i['name']} × {i['qty']}  ¥{i['price']}"
            for i in order.get("items", [])
        )
        return f"""订单号：{order['order_id']}
状态：{order['status_text']}
下单时间：{order['created_at']}
商品：
{items_text}
总金额：¥{order['total_amount']}"""

    @staticmethod
    def _format_logistics(logistics: dict) -> str:
        if not logistics:
            return ""
        tracks = "\n".join(
            f"  {t['time']} {t['content']}"
            for t in logistics.get("tracks", [])[:5]
        )
        return f"""快递公司：{logistics['company']}
运单号：{logistics['tracking_no']}
状态：{logistics['status']}
预计送达：{logistics.get('estimated_delivery', '未知')}
最新轨迹：
{tracks}"""
```

---

## 四、知识库示例

### 4.1 退换货政策（knowledge/退换货政策.md）

```markdown
# 退换货政策

## 七天无理由退货
- 自签收之日起7天内可申请无理由退货
- 商品须保持完好，不影响二次销售
- 退货运费由买家承担（质量问题除外）

## 不支持退货的情况
- 定制商品、个人卫生用品
- 已使用/损坏/影响二次销售的商品
- 超过7天无理由退货期限
- 虚拟商品（充值卡、会员等）

## 退款时效
| 支付方式 | 退款到账时间 |
|---------|------------|
| 微信支付 | 1-3个工作日 |
| 支付宝   | 1-3个工作日 |
| 银行卡   | 3-7个工作日 |
| 花呗/信用卡 | 3-7个工作日 |

## 质量问题处理
- 签收后48小时内反馈质量问题
- 需提供商品照片作为凭证
- 确认质量问题后，退货运费由商家承担
- 可选择退款或换货

## 运费说明
- 非质量问题退货：买家承担退货运费
- 质量问题退货：商家承担运费
- 商家发错货：商家承担运费
```

### 4.2 精确 QA 对（knowledge/qa_pairs.json）

```json
[
  {
    "q": "多久能收到货",
    "a": "下单后1-2天内发货，一般3-5天可收到。偏远地区可能需要5-7天。您可以在订单详情中查看物流进度。"
  },
  {
    "q": "可以修改收货地址吗",
    "a": "未发货的订单可以修改地址，请提供订单号，我帮您修改。已发货的订单无法修改地址，建议您联系快递员协商。"
  },
  {
    "q": "发票怎么开",
    "a": "您可以在订单详情页申请电子发票，支持增值税普通发票。如需增值税专用发票，请提供公司名称、税号、地址、电话和开户行信息。"
  },
  {
    "q": "优惠券怎么用",
    "a": "在结算页面，选择可用的优惠券即可抵扣。优惠券有使用门槛和有效期，请注意查看使用条件。"
  },
  {
    "q": "密码忘了怎么办",
    "a": "您可以通过注册手机号重置密码：点击登录页的「忘记密码」→ 输入手机号 → 获取验证码 → 设置新密码。"
  }
]
```

---

## 五、与八字插件的对照（面试重点）

| 维度 | 八字命理插件 | 电商售后插件 | 平台层统一处理 |
|------|------------|------------|--------------|
| **意图数** | 3-5 个 | 15+ 个 | 意图引擎通用 |
| **槽位类型** | 日期、时辰、性别 | 订单号、手机号、金额、图片 | 槽位引擎通用 |
| **外部集成** | 无（纯计算） | 订单API、物流API、支付API



